[package]
name = "fs-size"
version = "0.1.0"
edition = "2024"

[dependencies]
tempfile = "3.27.0"

[target.'cfg(windows)'.dependencies]
windows = { version = "0.62", features = [
    "Win32_Storage_FileSystem",
    "Win32_Foundation"
] }
